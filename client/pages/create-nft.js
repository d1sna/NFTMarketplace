import React from 'react';
// import PropTypes from 'prop-types';

const CreateNFT = () => (
  <div className="text-white ">CreateNFT</div>
);

// CreateNFT.propTypes = {
//   firstProp: PropTypes.shape().isRequired,
// };

export default CreateNFT;
